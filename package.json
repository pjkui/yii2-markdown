{
  "name": "yii2-markdown-tests",
  "version": "0.0.0",
  "private": true,
  "description": "Test harness for pjkui/yii2-markdown (Jest unit + Playwright e2e).",
  "license": "Apache-2.0",
  "scripts": {
    "test": "jest",
    "test:e2e": "playwright test",
    "test:php": "vendor/bin/phpunit",
    "test:all": "composer install --no-interaction --no-progress && npm install --no-audit --no-fund && npm run test:php && npm test && npm run test:e2e"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0",
    "marked": "^12.0.2",
    "turndown": "^7.2.0",
    "turndown-plugin-gfm": "^1.0.2",
    "vditor": "^3.10.0"
  }
}
